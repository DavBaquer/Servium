import { FilterTransaccionPipe } from './filter-transaccion.pipe';

describe('FilterTransaccionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTransaccionPipe();
    expect(pipe).toBeTruthy();
  });
});
