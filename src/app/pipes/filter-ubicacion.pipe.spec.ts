import { FilterUbicacionPipe } from './filter-ubicacion.pipe';

describe('FilterUbicacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUbicacionPipe();
    expect(pipe).toBeTruthy();
  });
});
